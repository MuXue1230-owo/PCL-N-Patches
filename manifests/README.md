# manifests/

CI / local generation writes per-variant JSON here under `artifacts/<tag>/manifests/`.

Committed samples (if any) are for documentation only. Authoritative data lives on **GitHub Releases** of this repository (`index.json` + `patches/`).
